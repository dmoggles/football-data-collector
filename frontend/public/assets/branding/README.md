# TapLine Branding Assets

Place local branding images here (for example: `logo.svg`, `logo.png`, `icon-192.png`).

Because this is under `frontend/public`, files can be referenced directly in the app by path:
- `/assets/branding/logo.svg`
- `/assets/branding/logo.png`
